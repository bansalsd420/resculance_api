# Frontend production environment for Resculance
# Do NOT commit this file with real secrets. Use placeholders if storing in repo.

# Base API URL the frontend will call (must include /api/v1 if backend routes are under that path)
VITE_API_URL=https://resculance.gapsheight.com/api/v1

# Socket URL used by socketService (use wss:// if you force secure WebSocket, but http(s) works for socket.io)
VITE_SOCKET_URL=https://resculance.gapsheight.com

# Frontend origin (useful for any client-side checks)
VITE_APP_ORIGIN=https://resculance.gapsheight.com

# App display name
VITE_APP_TITLE=Resculance

# Toggle analytics or feature flags
VITE_ENABLE_ANALYTICS=false
